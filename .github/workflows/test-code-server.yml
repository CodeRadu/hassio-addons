name: 'Test'

on:
  push:
    branches: [dev]
    paths:
      - code-server/**

jobs:
  build:
    name: Test build code-server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3
      - name: Test build code-server
        uses: home-assistant/builder@master
        with:
          args: |
            --test \
            --all \
            --target code-server \
            --docker-hub ${{ github.actor }}
